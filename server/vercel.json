{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "installCommand": "echo skip-server-install",
  "buildCommand": "cd ../web && npm install --include=dev --ignore-scripts && npm run build -- --outDir ../server/dist",
  "outputDirectory": "dist",
  "rewrites": [{ "source": "/((?!assets/).*)", "destination": "/index.html" }]
}
